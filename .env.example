WEBCAM_INDEX=0                       # Webcam device index (usually 0 for the default camera)
MODEL_PATH=models/yolov8n.pt            # Path to the YOLOv8 model weights file
CONFIDENCE_THRESHOLD=0.5               # Minimum detection confidence score (0.0–1.0)
SMTP_HOST=smtp.example.com             # SMTP server hostname
SMTP_PORT=587                          # SMTP server port (e.g. 587 for TLS, 465 for SSL)
SMTP_USER=Pradeepargal22@gmail.com          # SMTP login username / sender email address
SMTP_PASSWORD=xsohytuhzoyzrhlr
SUPERVISOR_EMAIL=supervisor@example.com # Email address to send violation reports to
LOG_DIR=logs                           # Directory where hourly log files will be written
